// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  reactStrictMode: true,
  basePath: "/string-extract",
  distDir: "out",
};

module.exports = nextConfig;
